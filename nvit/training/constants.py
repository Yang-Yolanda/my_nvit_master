"""Shared defaults for guided training (keep in sync with experiment YAML when needed)."""

# If Hydra omits GENERAL.CHECKPOINT_EVERY_N_TRAIN_STEPS, use this.
DEFAULT_CHECKPOINT_EVERY_N_TRAIN_STEPS = 100
